# =============================================================
#  The Walking Ways — Path B: Mantling Sheogorath
#  Namespace: mantling_sheogorath
#
#  EVENTS IN THIS FILE:
#  ────────────────────
#  mantling_sheogorath.intro      — Path chosen
#  mantling_sheogorath.rank1      — Seeking (madness, obelisk)
#  mantling_sheogorath.rank2      — Striving (realm, greymarch)
#  mantling_sheogorath.rank3      — Threshold (30% possession risk)
#  mantling_sheogorath.apotheosis — Apotheosis: the new prince
#  mantling_sheogorath.possession — Failure: madness claims the mantler
#
#  LORE BASIS:
#  ───────────
#  Sheogorath was once Jyggalag, Prince of Order — cursed by
#  the other Daedric Princes to become his opposite: the Prince
#  of Madness. The Hero of Kvatch mantled him in 3E 433 during
#  the Greymarch, when Jyggalag returned to destroy his own realm
#  and the Hero defeated him, allowing Jyggalag to finally be
#  free — and themselves to become Sheogorath.
#  Madness is not the absence of reason. It is a reason the
#  rest of creation cannot yet follow.
#  Possession risk: the Grey March does not pause for mantlers.
#  [SOURCE: CANON — Shivering Isles DLC; UESP Lore:Sheogorath]
# =============================================================

namespace = mantling_sheogorath

# ═══════════════════════════════════════════════════════════════
#  SHEOGORATH — THE PRINCE OF MADNESS
# ═══════════════════════════════════════════════════════════════

mantling_sheogorath.intro = {
    type = character_event
    title = mantling_sheogorath.intro.t
    desc  = mantling_sheogorath.intro.desc
    theme = intrigue

    option = {
        name = mantling_sheogorath.intro.a
        add_stress = 20
        add_piety = 25
        add_character_modifier = {
            modifier = ww_path_mantling_active
            years = 100
        }
        add_character_modifier = {
            modifier = mantling_daedric_risk
            years = 100
        }
    }
}

mantling_sheogorath.rank1 = {
    type = character_event
    title = mantling_sheogorath.rank1.t
    desc  = mantling_sheogorath.rank1.desc
    theme = intrigue

    option = {
        name = mantling_sheogorath.rank1.a
        add_prestige = 100
        add_stress = 10
    }

    option = {
        name = mantling_sheogorath.rank1.b
        # The cheese option — Sheogorath always has cheese
        add_prestige = 75
        add_stress = -5
        add_piety = -10   # irreverence has a cost with the orthodox
    }
}

mantling_sheogorath.rank2 = {
    type = character_event
    title = mantling_sheogorath.rank2.t
    desc  = mantling_sheogorath.rank2.desc
    theme = intrigue

    option = {
        name = mantling_sheogorath.rank2.a
        add_prestige = 150
        add_stress = 15
    }

    option = {
        name = mantling_sheogorath.rank2.b
        add_prestige = 125
        add_stress = 5
        add_dread = 5
    }
}

mantling_sheogorath.rank3 = {
    type = character_event
    title = mantling_sheogorath.rank3.t
    desc  = mantling_sheogorath.rank3.desc
    theme = intrigue

    option = {
        name = mantling_sheogorath.rank3.a   # "The Greymarch cannot claim what it already made."
        add_prestige = 200
        add_stress = 20
        add_piety = 50
    }
}

mantling_sheogorath.apotheosis = {
    type = character_event
    title = mantling_sheogorath.apotheosis.t
    desc  = mantling_sheogorath.apotheosis.desc
    theme = intrigue

    trigger = {
        has_character_flag = ww_mantling_sheogorath_active
        var:ww_mantling_sheogorath_rank >= 4
        NOT = { has_character_flag = ww_mantling_sheogorath_completed }
        NOT = { has_character_flag = ww_daedric_mantle_failed }
    }

    immediate = {
        complete_path_mantling_sheogorath = yes
    }

    option = {
        name = mantling_sheogorath.apotheosis.a   # "The cheese has been found. The realm is mine."
        add_prestige = 400
        add_piety = 150
        add_dread = 20
    }

    option = {
        name = mantling_sheogorath.apotheosis.b   # "I am the new Prince. I am also still myself."
        add_prestige = 350
        add_stress = -30
    }
}

mantling_sheogorath.possession = {
    type = character_event
    title = mantling_sheogorath.possession.t
    desc  = mantling_sheogorath.possession.desc
    theme = intrigue

    trigger = {
        has_character_flag = ww_daedric_mantle_failed
        has_character_flag = ww_mantling_sheogorath_active
    }

    immediate = {
        # The Grey March claims the mantler — deep madness, not apotheosis
        remove_trait = walking_ways_seeker
        clear_all_ww_active_flags = yes
        remove_variable = ww_mantling_sheogorath_rank
        remove_variable = ww_mantling_sheogorath_milestones
        remove_character_flag = ww_daedric_mantle_failed
        add_trait = lunatic
        add_trait = possessed
    }

    option = {
        name = mantling_sheogorath.possession.a   # "Not Sheogorath. Not myself. Something in between."
        add_stress = 40
        add_prestige = 75
        # Still some power, but not the principled madness sought
    }
}

# ═══════════════════════════════════════════════════════════════
#  SHEOGORATH MILESTONE EVENTS
# ═══════════════════════════════════════════════════════════════

mantling_sheogorath.milestone_greymarch = {
    type = character_event
    title = mantling_sheogorath.milestone_greymarch.t
    desc = mantling_sheogorath.milestone_greymarch.desc
    theme = intrigue

    trigger = {
        has_character_flag = ww_mantling_sheogorath_active
        NOT = { has_character_flag = ww_mantling_sheogorath_milestone_greymarch }
        OR = {
            has_trait = jyggalag_champion
            has_trait = jyggalag_ascendant
            intrigue >= 16
        }
    }

    option = {
        name = mantling_sheogorath.milestone_greymarch.a
        set_character_flag = ww_mantling_sheogorath_milestone_greymarch
        add_piety = 75
        add_dread = 15
        add_prestige = 100
    }
    option = {
        name = mantling_sheogorath.milestone_greymarch.b
        set_character_flag = ww_mantling_sheogorath_milestone_greymarch
        add_stress = -20
        add_intrigue_xp = 150
    }
}

mantling_sheogorath.milestone_jyggalag_defeated = {
    type = character_event
    title = mantling_sheogorath.milestone_jyggalag_defeated.t
    desc = mantling_sheogorath.milestone_jyggalag_defeated.desc
    theme = intrigue

    trigger = {
        has_character_flag = ww_mantling_sheogorath_active
        NOT = { has_character_flag = ww_mantling_sheogorath_milestone_jyggalag_defeated }
        OR = {
            has_trait = jyggalag_ascendant
            AND = {
                martial >= 16
                prestige >= 300
            }
        }
    }

    option = {
        name = mantling_sheogorath.milestone_jyggalag_defeated.a
        set_character_flag = ww_mantling_sheogorath_milestone_jyggalag_defeated
        add_prestige = 175
        add_dread = 20
    }
    option = {
        name = mantling_sheogorath.milestone_jyggalag_defeated.b
        set_character_flag = ww_mantling_sheogorath_milestone_jyggalag_defeated
        add_piety = 100
        add_learning_xp = 125
    }
}

mantling_sheogorath.milestone_madness = {
    type = character_event
    title = mantling_sheogorath.milestone_madness.t
    desc = mantling_sheogorath.milestone_madness.desc
    theme = intrigue

    trigger = {
        has_character_flag = ww_mantling_sheogorath_active
        NOT = { has_character_flag = ww_mantling_sheogorath_milestone_madness }
        stress >= 25
    }

    option = {
        name = mantling_sheogorath.milestone_madness.a
        set_character_flag = ww_mantling_sheogorath_milestone_madness
        add_stress = -20
        add_intrigue_xp = 150
    }
    option = {
        name = mantling_sheogorath.milestone_madness.b
        set_character_flag = ww_mantling_sheogorath_milestone_madness
        add_stress = -15
        add_dread = 10
        add_piety = 50
    }
}

mantling_sheogorath.milestone_new_prince = {
    type = character_event
    title = mantling_sheogorath.milestone_new_prince.t
    desc = mantling_sheogorath.milestone_new_prince.desc
    theme = intrigue

    trigger = {
        has_character_flag = ww_mantling_sheogorath_active
        NOT = { has_character_flag = ww_mantling_sheogorath_milestone_new_prince }
        prestige >= 300
        intrigue >= 14
    }

    option = {
        name = mantling_sheogorath.milestone_new_prince.a
        set_character_flag = ww_mantling_sheogorath_milestone_new_prince
        add_prestige = 175
        add_piety = 75
    }
    option = {
        name = mantling_sheogorath.milestone_new_prince.b
        set_character_flag = ww_mantling_sheogorath_milestone_new_prince
        add_intrigue_xp = 150
        add_prestige = 125
    }
}

mantling_sheogorath.milestone_obelisk = {
    type = character_event
    title = mantling_sheogorath.milestone_obelisk.t
    desc = mantling_sheogorath.milestone_obelisk.desc
    theme = intrigue

    trigger = {
        has_character_flag = ww_mantling_sheogorath_active
        NOT = { has_character_flag = ww_mantling_sheogorath_milestone_obelisk }
        OR = {
            learning >= 14
            piety >= 200
        }
        prestige >= 200
    }

    option = {
        name = mantling_sheogorath.milestone_obelisk.a
        set_character_flag = ww_mantling_sheogorath_milestone_obelisk
        add_prestige = 150
        add_piety = 75
    }
    option = {
        name = mantling_sheogorath.milestone_obelisk.b
        set_character_flag = ww_mantling_sheogorath_milestone_obelisk
        add_learning_xp = 150
        add_stress = -15
    }
}

mantling_sheogorath.milestone_realm = {
    type = character_event
    title = mantling_sheogorath.milestone_realm.t
    desc = mantling_sheogorath.milestone_realm.desc
    theme = intrigue

    trigger = {
        has_character_flag = ww_mantling_sheogorath_active
        NOT = { has_character_flag = ww_mantling_sheogorath_milestone_realm }
        realm_size >= 10
        intrigue >= 12
    }

    option = {
        name = mantling_sheogorath.milestone_realm.a
        set_character_flag = ww_mantling_sheogorath_milestone_realm
        add_prestige = 150
        add_dread = 15
    }
    option = {
        name = mantling_sheogorath.milestone_realm.b
        set_character_flag = ww_mantling_sheogorath_milestone_realm
        add_prestige = 125
        add_piety = 75
    }
}
